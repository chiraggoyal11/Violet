# Copy to frontend/.env and fill in your Google OAuth client ID.
# Must match GOOGLE_CLIENT_ID in config/config.env on the API server.
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com